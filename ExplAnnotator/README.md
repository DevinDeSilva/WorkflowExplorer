# ExplAnnotator

Explanation Annotator is a library used to create a Knowledge Graph (KG) from provenance execution traces of other AI applications.

## Installation

```bash
pip install .
```

## Usage

```python
from expl_annotator import ProvOneManager

# Initialize with config
manager = ProvOneManager("config.yaml")

# Use manager methods...
```
